{
  "name": "inventory-scanner-local",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Mac-hosted inventory scanner web app with SQLite storage, iPhone/PWA scanning, Excel export, backups, and demo mode.",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node --experimental-sqlite server/index.js",
    "dev": "node --experimental-sqlite server/index.js",
    "test": "node --experimental-sqlite --test tests/*.test.js"
  },
  "keywords": [
    "inventory",
    "barcode",
    "scanner",
    "sqlite",
    "pwa",
    "excel"
  ],
  "license": "MIT"
}
